void cgst1(int price)
{
float cgst=(2.5/100)*price;
printf("the cgst for the product is %f\n",cgst);
}
void cgst2(int price)
{
float cgst=(6/100)*price;
printf("the cgst for the product is %f\n",cgst);
}
void cgst3(int price)
{
float cgst=(9/100)*price;
printf("the cgst for the product is %f\n",cgst);
}
